- implement check wether the rgba values in class Pixel are in range 0<n<255 (comes from bmp standard) DONE
- the getPixel method should return a const Pixel& object!!! - DONE (PICTURE)
- filter class should have neccessary data for applying it - NOT TASK, DIRECTION
- user interface - DONE (IFILTER)
- put ImageFiltering classes into namespace - IN PROGRESS
- update the cmakelists.txt to the correct file structure - DONE
- create user input handler (class?) funcions - IN PROGRESS, LATER




create new object:
std::unique_ptr<ClassType> varname = \
    std::make_unique<ClassName>;

    shared: same

if there are more cpp files, which declare things in the same namespace, that is not a problem, this work, everything will be in the namespace



DONE
====

- Pixel.hpp
- Picture.hpp
- IFilter.hpp
- ContrastBrightnessFilter.hpp

NEXT:
========
- create and write the Kernel.cpp (IMPORTANT) 
- create and write IConvolutionBasedFilter.cpp (IMPORTANT) // i have to apply the convolution logic here
- create a ConvolutionFilter class, which will do all the convolution based filtering, and a library for the basic kernels. allow dynamic kernel usage also (details in BlurFilter.hpp) 
    and delete all convolution based filter classes (blur, sharpen, etc.)
